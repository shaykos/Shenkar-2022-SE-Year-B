//יצירת הפונקציות 

const Sum = (a, b) => { return a + b; }

const Multiply = (a, b) => { return a * b; }

const Divider = (a, b) => { return a / b; }

const Sub = (a, b) => { return a - b; }

//מייצא את הפונקציות כמודול
module.exports = { Sum, Multiply, Divider, Sub }